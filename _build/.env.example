# fortune0 secrets - NEVER commit the real .env file
# Copy this to .env and fill in real values

# Your Cloudflare Worker admin key (for viewing submissions, syncing subscribers)
# Set this in Cloudflare: wrangler secret put ADMIN_KEY
# Also export locally for sync scripts: export FORTUNE0_ADMIN_KEY=xxx
FORTUNE0_ADMIN_KEY=

# Stripe is handled via CLI login, not stored here
# Just run: stripe login
# Never put sk_live_xxx in any file

# Worker URL (public, not a secret)
WORKER_URL=https://fortune0-forms.mattmauersp.workers.dev
