version: "3"
services:
  fortune0:
    build: .
    ports:
      - "8080:8080"
    environment:
      - F0_PORT=8080
      - F0_LICENSE_SECRET=${F0_LICENSE_SECRET:-fortune0-dev-secret-2026}
    volumes:
      - ./data:/app/data
      - .:/app
    restart: unless-stopped
